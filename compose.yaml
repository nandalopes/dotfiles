name: yadr
services:
  dotfiles:
    environment:
      IMAGE_NAME: &imageTag "yadr:mine"
    container_name: yadr-mine
    build:
      context: .
      args:
        DEBIAN_FRONTEND: noninteractive
      tags:
        - *imageTag
    image: *imageTag
    volumes:
      - ./:/root/.yadr/
      - plugged:/root/.yadr/root/vim/plugged
      - zprezto:/root/.zprezto
volumes:
  plugged:
  zprezto:
    external: true
