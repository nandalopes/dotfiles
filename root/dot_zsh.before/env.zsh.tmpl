#!/bin/zsh

{{ template "shell/user_path" "" }}
{{ template "shell/yadr_path" . }}

export HIST_IGNORE_ALL_DUPS=1

### append zsh completions
# append completions to fpath
fpath=(
  "${(@)fpath}"
  $HOME/.local/share/zsh/site-functions(N)
)
