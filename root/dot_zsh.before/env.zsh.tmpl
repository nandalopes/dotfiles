#!/bin/zsh

{{ template "shell/user_path" "export " }}

{{ template "shell/asdf" . }}

### append zsh completions
# append completions to fpath
fpath=(
  "${(@)fpath}"
  $HOME/.local/share/zsh/site-functions(N)
)

